[project]
name = "the-bear-down-bots"
version = "0.1.0"
description = "A project for the Bear Down Bots."
authors = ["Alex R. <alexr@example.com>"]
license = "MIT"
readme = "README.md"
keywords = ["robotics", "automation", "ECE-579"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"